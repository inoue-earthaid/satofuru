import re
from pathlib import Path

# base_dir = Path(r'C:\Users\ooaka\Downloads')
# pattern = '^numazu.*\.csv$'
# filterd_files = list(filter(lambda x: re.match(pattern, x.name), base_dir.iterdir()))
# if len(filterd_files) != 1:
#     try:
#         raise Exception('file.length is not 1file')
#     except Exception as e:
#         print(e)
# else:
#     print(str(filterd_files[0]))

